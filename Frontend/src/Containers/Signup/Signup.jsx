import { useNavigate } from "react-router";
const Signup = () => {
  const navigate = useNavigate();
  return (
    <>
      <div className="main-signup-container"></div>
    </>
  );
};

export default Signup;

{
  /* <button
          onClick={() => {
            navigate("/Homepage");
          }}
        >
          Homepage
        </button> */
}
